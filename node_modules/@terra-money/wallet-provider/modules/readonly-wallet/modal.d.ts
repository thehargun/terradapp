import { NetworkInfo } from '@terra-money/use-wallet';
import { ReadonlyWalletSession } from './types';
interface Options {
    networks: NetworkInfo[];
    className?: string;
}
export declare function readonlyWalletModal({ networks, className, }: Options): Promise<ReadonlyWalletSession | null>;
export {};
