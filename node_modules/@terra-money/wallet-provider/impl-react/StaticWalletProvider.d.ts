import { TerraWebExtensionFeatures } from '@terra-dev/web-extension-interface';
import { Connection, ConnectType, NetworkInfo, WalletInfo, WalletStatus } from '@terra-money/use-wallet';
import { ReactNode } from 'react';
export interface StaticWalletProviderProps {
    children: ReactNode;
    defaultNetwork: NetworkInfo;
    status?: WalletStatus;
    availableConnectTypes?: ConnectType[];
    availableInstallTypes?: ConnectType[];
    availableConnections?: Connection[];
    wallets?: WalletInfo[];
    supportFeatures?: Set<TerraWebExtensionFeatures>;
}
export declare function StaticWalletProvider({ children, defaultNetwork, status, availableConnectTypes, availableInstallTypes, availableConnections, wallets, supportFeatures, }: StaticWalletProviderProps): JSX.Element;
